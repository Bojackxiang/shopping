import NewProductView from '@/features/product-editor/views/new-product-view';

import React from 'react';

export const metadata = {
  title: 'Dashboard: Create New Product'
};

const page = () => {
  return <NewProductView />;
};

export default page;
